fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pillow==10.3.0
numpy==1.26.4
huggingface_hub==0.23.0
transformers==4.40.0
sentencepiece==0.2.0
timm==0.4.12
torchscale==0.2.0
torchmetrics==1.3.2
tensorboardX==2.6.2.2

# CPU-only PyTorch build (much smaller image, no CUDA needed on Cloud Run).
# If you later move to a GPU host, replace these two lines with the
# CUDA-enabled build matching that host's CUDA version.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2
